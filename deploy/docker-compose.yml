services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: mybox
      POSTGRES_USER: mybox
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-change-me-local-only}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mybox -d mybox"]
      interval: 5s
      timeout: 5s
      retries: 12
    volumes:
      - postgres_data:/var/lib/postgresql/data

  caddy:
    image: caddy:2.10-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config

  mybox:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    restart: unless-stopped
    depends_on:
      - mybox-api
    # Unified frontend + API origin for local HTTPS tunnel testing (for
    # example, `cloudflared tunnel run --url http://127.0.0.1:8081`). Keep it
    # loopback-only on the host.
    ports:
      - "${MYBOX_FRONTEND_PORT:-127.0.0.1:8081}:80"

  mybox-api:
    build:
      context: ..
      dockerfile: deploy/server.Dockerfile
    restart: unless-stopped
    env_file:
      - ../.env
    environment:
      DATABASE_URL: postgres://mybox:${POSTGRES_PASSWORD:-change-me-local-only}@postgres:5432/mybox
    depends_on:
      postgres:
        condition: service_healthy
    # Loopback binding supports local Trunk development on :8080 without
    # exposing the authenticated API to the public network.
    ports:
      - "127.0.0.1:3000:3000"

  # journal: second SMBL product
  # (in its own compose once its source + domain + secrets are confirmed;
  #  caddy routes journal.<domain> -> journal:80 when enabled)
  journal:
    image: nginx:1.27-alpine
    restart: unless-stopped
    entrypoint: ["sh", "-c", "echo 'journal placeholder - source pending'; sleep infinity"]

volumes:
  caddy_data:
  caddy_config:
  postgres_data:
