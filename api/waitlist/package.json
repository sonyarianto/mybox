{
  "name": "waitlist-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "migrate:local": "wrangler d1 migrations apply mybox-waitlist --local",
    "migrate:remote": "wrangler d1 migrations apply mybox-waitlist --remote"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
